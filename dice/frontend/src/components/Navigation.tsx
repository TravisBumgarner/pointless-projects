import { Link } from "react-router";

const Navigation = () => {
  return (
    <nav>
      <ul>
        <li>
          <Link to="/">Ditzy Dice</Link>
        </li>
      </ul>
    </nav>
  );
};

export default Navigation;
